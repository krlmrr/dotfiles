# Herd
export PATH="$HOME/Library/Application Support/Herd/bin:$PATH"

alias a="php artisan"

alias brewup="brew upgrade && brew upgrade --greedy && brew cleanup --prune=all && sudo yabai --uninstall-sa && sudo yabai --install-sa && sudo yabai --load-sa && yabai --restart-service && sudo bash ~/Code/dotfiles/mac/tcc-cleanup.sh && open /Applications/Raycast.app/"

