# Herd
export PATH="$HOME/Library/Application Support/Herd/bin:$PATH"

alias a="php artisan"

alias brewup="brew upgrade && brew upgrade --greedy && brew cleanup --prune=all && sudo bash ~/Code/dotfiles/mac/tcc-cleanup.sh && open /Applications/Raycast.app/"

