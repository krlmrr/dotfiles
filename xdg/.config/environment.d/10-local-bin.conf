# Omarchy's env-bootstrap appends ~/.local/bin, which puts it behind /usr/bin.
# The screensaver overrides in the linuxbin package shadow /usr/bin/omarchy-*
# by name, so they only win if ~/.local/bin comes first — including in the
# graphical session, which is where the idle daemon launches them.
PATH=$HOME/.local/bin:$PATH
