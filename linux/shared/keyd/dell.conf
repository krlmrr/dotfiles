[ids]
0001:0001

[main]
capslock = overload(control, esc)
pageup = noop
pagedown = noop
leftalt = layer(control)
leftcontrol = layer(alt)

# Mac-style line nav: Ctrl+Left/Right -> Home/End
# (On Dell, leftalt is the control layer, so this is alt+arrow -> home/end)
[control]
left = home
right = end
