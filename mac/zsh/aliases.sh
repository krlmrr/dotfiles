# Herd
export PATH="$HOME/Library/Application Support/Herd/bin:$PATH"

alias a="php artisan"

alias brewup="brew upgrade && brew upgrade --greedy && brew cleanup --prune=all && open /Applications/Raycast.app/"

